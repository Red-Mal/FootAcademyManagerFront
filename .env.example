# Base URL of the backend API. In dev, Vite proxies /api to localhost:8080
# (see vite.config.ts). In production (Railway), pass the backend's full
# public URL as a Docker build arg, e.g.
# https://academy-backend.up.railway.app/api
VITE_API_BASE_URL=/api
VITE_APP_NAME=CRN Académie
