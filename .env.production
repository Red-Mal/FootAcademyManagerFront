# Default for local `vite build` / `vite preview` (same-origin reverse proxy).
# For the Railway Docker build, override via --build-arg VITE_API_BASE_URL
# with the backend's full public URL, e.g.
# https://academy-backend.up.railway.app/api
VITE_API_BASE_URL=/api
VITE_APP_NAME=CRN Académie
